// (c) 2026 GoElev8.ai | Aaron Bryant. All rights reserved.
//
// Konquered Kocktails is a single-page site: one route (/), one API route
// (/api/checkout/kbsetup), all styling inline, all video on Mux. Nothing here
// needs rewrites, redirects, or image remote patterns.

/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
